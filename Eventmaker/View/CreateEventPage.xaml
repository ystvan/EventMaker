<Page
    x:Class="Eventmaker.View.CreateEventPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eventmaker.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModel="using:Eventmaker.ViewModel"
    mc:Ignorable="d">

    
    <!--Remember to define the DatatContext-->
    
    <Page.DataContext>
      <viewModel:EventViewModel/>  
    </Page.DataContext>
    
    <!--By using static style resources we can ensure a steady design flow and save up quite a lot typing--> 
    
    <Page.Resources>
        <Style TargetType="TextBlock" x:Key="CreateEventTextBlockStyle">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,5"/>
            <Setter Property="FontFamily" Value="Calibri Light"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="TextBox" x:Key="CreateEventTextBoxStyle">
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
            <Setter Property="FontFamily" Value="Calibri Light" />
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
        <Style TargetType="Button" x:Key="CreateEventButtonStyle">
            <Setter Property="BorderBrush" Value="DarkSeaGreen"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Calibri Light"/>
        </Style>
        <Style TargetType="StackPanel" x:Key="VerticalStackPanel">
            <Setter Property="Orientation" Value="Vertical"/>
        </Style>
        <Style TargetType="StackPanel" x:Key="HorizontalStackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
        </Style>
    </Page.Resources>
    
    <!-- By having an adaptive design, our app is able to be used on different devices with various screen sizes -->
    
    <VisualStateManager.VisualStateGroups>
        <VisualStateGroup x:Name="VisualStateGroup">
            
            <VisualState x:Name="MinimumLayout">
                <VisualState.StateTriggers>
                    <AdaptiveTrigger MinWindowWidth="0"/>
                </VisualState.StateTriggers>
                <VisualState.Setters>
                    <Setter Target="HeaderTextBlock.TextSize" Value="20"></Setter>
                </VisualState.Setters>
            </VisualState>
            
        </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>
    
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
       <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
       </Grid.ColumnDefinitions>
       <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
       </Grid.RowDefinitions> 
       
        <TextBlock x:Name="HeaderTextBlock" Grid.Row="0" Grid.Column="1" 
                   Text="Add New Event" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontWeight="Bold"
                   />
        
        <StackPanel Grid.Column="1" Grid.Row="1" Style="{StaticResource VerticalStackPanel}">
        
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" 
                           Text="Id"/>
                <TextBox Style="{StaticResource CreateEventTextBoxStyle}" 
                         Text="{Binding Id, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" 
                           Text="Name"/>
                <TextBox Style="{StaticResource CreateEventTextBoxStyle}" 
                         Text="{Binding Name, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" 
                           Text="Description"/>
                <TextBox Style="{StaticResource CreateEventTextBoxStyle}" 
                         Text="{Binding Description, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" 
                           Text="Place"/>
                <TextBox Style="{StaticResource CreateEventTextBoxStyle}" 
                         Text="{Binding Place, Mode=TwoWay}"/>
            </StackPanel>
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" 
                           Text="Date"/>
                <DatePicker Date="{Binding Date}"/>
            </StackPanel>
            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <TextBlock Style="{StaticResource CreateEventTextBlockStyle}" Text="Time"/>
                <TimePicker ClockIdentifier="24HourClock"
                            Time="{Binding Time}"/>
            </StackPanel>

            <StackPanel Style="{StaticResource HorizontalStackPanel}">
                <Button Style="{StaticResource CreateEventButtonStyle}" Name="AddNewEvent" Content="Add" Command="{Binding CreateEventCommand}"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</Page>
